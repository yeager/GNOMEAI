# Command Shelf

Store named command snippets locally and copy one explicitly when needed. Command Shelf never runs commands. Its data is kept at `$XDG_STATE_HOME/command-shelf/items.json`.

```sh
python3 command_shelf.py
```
