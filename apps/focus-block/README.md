# Focus Block

A local focus and break timer. Set the focus and break lengths, start the timer, and receive a desktop notification when the phase changes. Settings are stored under `$XDG_STATE_HOME/focus-block/`.

```sh
python3 focus_block.py
```
